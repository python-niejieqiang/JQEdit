["d\"", "ImmersiveNotepad", "mmersiveNotepad(QMainWindow):\n    def __init__(self):\n        super().__init__()\n\n        self.setWindowTitle(\"Immersive Notepad\")\n        self.setGeometry(100, 100, 800, 600)  # 设置主窗口初始大小\n", "# 将窗口设置为全屏", "Notepad(QMainWindow):\n    def __init__(self):\n        super().__init__()\n\n        self.setWindowTitle(\"Immersive Notepad\")\n        self.setGeometry(100, 100, 800,", "设置主窗口初始大小"]